{
  "private": true,
  "name": "yaniv-frontend",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender --no-inline-css --dest ../public/frontend",
    "serve": "sirv build --cors --single",
    "dev": "preact watch",
    "lint": "eslint src",
    "fix": "eslint src --fix"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "node-sass": "^4.14.1",
    "preact-cli": "^3.0.0-rc.6",
    "sass-loader": "^8.0.2",
    "sirv-cli": "^0.4.5"
  },
  "dependencies": {
    "bootstrap": "^4.4.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^3.2.1",
    "react-smooth-dnd": "^0.11.1",
    "ts-loader": "^6.2.2"
  }
}
